<at:ObservableWindow x:Class="Antilli.MKInputBox" DataContext="{Binding RelativeSource={RelativeSource Self}}"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:at="clr-namespace:Antilli"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Title" Height="135" Width="300"
        Background="#252525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Vertical" Grid.ColumnSpan="2">
            <Label Name="PromptText" Content="Prompt text:" Padding="6,4"/>
            <TextBox Name="ValueText" Text="Hello world" Padding="2" Margin="5" MaxLength="255" MaxLines="1"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="1" Grid.Column="1">
            <StackPanel.Resources>
                <Style TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Type Button}}">
                    <Setter Property="Margin" Value="4"/>
                    <Setter Property="Padding" Value="10,0"/>
                </Style>
            </StackPanel.Resources>
            
            <Button Name="btnOk" Content="OK" IsDefault="True"/>
            <Button Name="btnCancel" Content="Cancel" IsCancel="True"/>
        </StackPanel>
    </Grid>
</at:ObservableWindow>
